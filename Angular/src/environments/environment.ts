// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  //Firebase
  firebase: {
    apiKey: "AIzaSyCjGyKLooot7-DRTpgQ9VeKkZNGTggCm-g",
    authDomain: "data-trees.firebaseapp.com",
    databaseURL: "https://data-trees.firebaseio.com",
    projectId: "data-trees",
    storageBucket: "data-trees.appspot.com",
    messagingSenderId: "219034751595",
    appId: "1:219034751595:web:d0de4f71ab3927a74c172e",
    measurementId: "G-DN9GFG301G"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
